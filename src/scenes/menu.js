Crafty.scene("menu",function(){
    /**
     * build main UI:
     * 1. have a main chat
     * 2. have a create room($optional)(have a number of preset rooms)
     * 3. have a select room to jon to
     **/
});